import { View } from 'ol';
import { IView } from '../../interfaces/map/gameandgis-map-view.interface';

export class GameandgisMapView extends View {
  constructor(props: IView) {
    super(props);
  }
}
